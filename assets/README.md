# Asset Handoff Notes

## Included references
- `reference/invoice-reference.jpg` — current invoice visual reference supplied by the user.
- `reference/ui-flow-mockup.png` — concept mockup generated during product planning.
- `reference/logo-primary-transparent.png` — transparent logo concept generated during planning.

## Important
The concept logo asset is a planning reference. Before production branding is locked, inspect it visually and replace it with the approved master logo if the shop already owns one.

The coding agent must not regenerate product/invoice visuals with AI during runtime.
